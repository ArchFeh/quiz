# 记录问题和 refs(作为问题的 tips)，但不可提供答案

== 补充问题

0) page 159 表9.3 MTIME为什么只设计一个寄存器，而MTIMECMP则是多个
MTIME寄存器只有一个，因为在处理器中，只需要保存当前的时间戳即可，而不需要保存多个时间戳。需要通过多个MTIMECMP寄存器支持多个计时器中断。
1) page 167 qemu virt 串口的中断10，在哪里可以查到这个信息？
书上表2.2 UART0
